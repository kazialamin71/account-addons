<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Periods, fiscal years, tax codes and financial report definitions are
             per-company configuration; assets, budgets and vouchers are per-company
             documents. One record rule shape covers them all. -->

        <record id="account_fiscalyear_comp_rule" model="ir.rule">
            <field name="name">Fiscal Year multi-company</field>
            <field name="model_id" ref="model_account_fiscalyear"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="account_period_comp_rule" model="ir.rule">
            <field name="name">Account Period multi-company</field>
            <field name="model_id" ref="model_account_period"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="account_tax_code_comp_rule" model="ir.rule">
            <field name="name">Tax Code multi-company</field>
            <field name="model_id" ref="model_account_tax_code"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="account_voucher_comp_rule" model="ir.rule">
            <field name="name">Voucher multi-company</field>
            <field name="model_id" ref="model_account_voucher"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="account_asset_asset_comp_rule" model="ir.rule">
            <field name="name">Asset multi-company</field>
            <field name="model_id" ref="model_account_asset_asset"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <record id="crossovered_budget_comp_rule" model="ir.rule">
            <field name="name">Budget multi-company</field>
            <field name="model_id" ref="model_crossovered_budget"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

    </data>
</odoo>
